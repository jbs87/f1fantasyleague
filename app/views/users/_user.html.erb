<tr>
	<td><%= user_counter+1 %></td>
	<td><%= link_to user.name, user_path(user) %></td>
	<td><%= user.current_score %></td>
	<td><% if current_user%>
		<% unless Friendship.exists?(current_user,user) || user == current_user%>
		<%= link_to "Add #{user.name}",
		friendships_path(:id => user.id), :method=> :post, remote: true %>
		<% end %>
		<% end %>
	</td>
</tr>